export interface GameModel {
  code: string;
  label: string;
  style: string;
  teamSize: number;
  hasPet: boolean;
  iconUrl: string;
  logoUrl: string;
  backgroundUrl: string;
  guideUrl: string;
  isActive: boolean;
}